# Copy this file to .env and fill in a real secret before running
# `docker compose up`. This same value must also be set as the
# JOBSPY_API_KEY environment variable (or credential) on the n8n side,
# and sent as the X-API-Key header when calling /scrape.
API_KEY=replace-with-a-long-random-secret
